const LoadingState = ({ label = "Loading" }) => (
  <div className="grid min-h-64 place-items-center rounded-lg border border-ink/10 bg-linen text-sm font-bold text-ink/60">{label}...</div>
);

export default LoadingState;
